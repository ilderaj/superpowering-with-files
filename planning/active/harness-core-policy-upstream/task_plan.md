# 任务计划：harness-core-policy-upstream

## 当前任务

执行 `docs/superpowers/plans/2026-04-11-harness-core-policy-upstream-plan.md` 的 Task 4: Add Platform Overrides。

## 范围

- 只创建 `harness/core/policy/platform-overrides/` 及其四个平台覆盖文件。
- 不修改 `harness/core/policy/base.md`。
- 不执行 Task 5 或后续任务。

## 完成标准

- 四个平台覆盖文件内容与计划一致。
- `npm run test:core` 通过。
- 本任务提交到 git，commit message 为 `feat: add platform policy overrides`。
