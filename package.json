{
  "name": "harness-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:core": "node --test tests/core/*.test.mjs",
    "verify": "node --test tests/core/*.test.mjs tests/installer/*.test.mjs"
  }
}
