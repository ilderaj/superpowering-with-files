{
  "name": "superpowering-with-files",
  "version": "1.0.10",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test",
    "test:mcp": "node --test --test-concurrency=1 tests/mcp/*.test.mjs",
    "test:core": "node --test tests/core/*.test.mjs",
    "verify:core": "node --test tests/core/*.test.mjs tests/runtime/*.test.mjs tests/installer/*.test.mjs tests/adapters/*.test.mjs tests/automation/*.test.mjs && node --test --test-concurrency=1 tests/mcp/*.test.mjs && node --test tests/plugin-kit/*.test.mjs",
    "preverify:homepage": "node scripts/ensure-homepage-deps.mjs",
    "verify:homepage": "npm run typecheck --prefix homepage && npm test --prefix homepage && npm run build --prefix homepage",
    "verify:all": "npm run verify:core && npm run verify:homepage",
    "verify": "npm run verify:core",
    "mcp:stdio": "node harness/mcp/stdio.mjs",
    "plugin:build": "node packages/plugin-kit/src/build-all.mjs",
    "plugin:verify": "node --test tests/plugin-kit/*.test.mjs",
    "plugin:smoke": "node packages/plugin-kit/src/smoke.mjs",
    "release:pack": "node packages/plugin-kit/src/build-all.mjs --release"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.20.0",
    "zod": "^3.25.76"
  }
}
