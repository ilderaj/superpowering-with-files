# Roadmap v1.1 Planning Hygiene

Active task path: `planning/active/roadmap-v1.1-planning-hygiene/`
Lifecycle state: active
Sync-back status: active, implementation in progress

## Objective

为 `planning/active/` 建立一个可验证、可操作的治理面：补 lifecycle audit checklist、补全量 active summary 命令，并用它收口 `project-roadmap-audit`。

## Implementation Slices

1. 增加 `./scripts/harness active-summary`：
   - 默认输出 operator-facing text summary
   - `--json` 输出结构化结果
   - `--output <path>` 可落地 JSON artifact
2. 更新 `docs/maintenance.md`：
   - 新增 `Planning Lifecycle Audit` 小节
   - 明确 checklist 与推荐命令
3. 更新 `docs/roadmap.md`：
   - 把 v1.1 成功标准改成可验证条目
4. 用 `active-summary` 审计 active queue，并据此 close/archive `project-roadmap-audit`
5. 运行 focused tests 与 repo verify，回写记录并 merge back `dev`

## Closeout Target

- active summary command 可用
- lifecycle audit runbook 可复用
- `project-roadmap-audit` archived
- `docs/roadmap.md` 的 v1.1 标记完成

## Current Status

- `active-summary` command 已实现并通过 focused tests。
- `project-roadmap-audit` 已归档到 `planning/archive/20260506-171553-project-roadmap-audit/`。
- `.harness/planning-active-summary.json` 当前显示：
  - total tasks: 12
  - active: 8
  - waiting_review: 4
  - looks_complete but intentionally retained: 4
- 剩余动作：运行 repo-level verify、提交 v1.1 分支、merge back `dev`、push `origin/dev`。
